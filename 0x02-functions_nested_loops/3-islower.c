#include <stdio.h>

/**
 * main -check the code
 *
 * isLower - checks for lowercase alphabet
 *
 * int c: alphabet to be checked
 *
 * Return: 0 if lowercase otherwise 1
 *
 */

int isLower(int c)
{
	int islower();

	if (islower(c))
	{
		return (1);
	}

	else
	{
		return (0);

	}
}

