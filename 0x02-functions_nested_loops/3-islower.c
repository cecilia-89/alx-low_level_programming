#include "main.h"
#include <stdlib.h>

/**
 * _islower - checks for lowercase alphabet
 *
 * Return: 0 if lowercase otherwise 1
 *
 */

int _islower(int c)
{
	int islower(int c);

	if (islower(c))
	{
		return (1);
	}

	else
	{
		return (0);

	}
}

