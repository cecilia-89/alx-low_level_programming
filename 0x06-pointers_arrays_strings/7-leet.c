#include "main.h"

/**
 * leet - encodes a string
 * str: the string to encode
 * Return: encoded string
 *
 */

char *leet(char *str)
{
	char *s, *strng;

	size_t length, i;

	length = strlen(str);

	s = "aeotl";

	strng = "43071";

	for (i = 0; i < length; i++)
	{
		for (j = 0; j < 5; j++)
		{
			if (str[i] == 

}
